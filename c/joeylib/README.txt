These files are my C++ library.
I think they still compile but it takes a long time to compile the whole lot.
Hence the make scripts, which parse the files and generate .h and .c files in the .comp directory.
