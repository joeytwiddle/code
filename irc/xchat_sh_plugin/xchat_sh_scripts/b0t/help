# if [ "$CHANNEL" = "#noobpug" ]
# then
	# ./pugbot/help
	# exit 0
# fi

./commands "$@"

# ./pugbot/help

# if [ "$CHANNEL" = "#utportal" ] || [ "$CHANNEL" = "#utb0t" ] || ! echo "$CHANNEL" | grep "^#" >/dev/null
# then
# else
# fi

