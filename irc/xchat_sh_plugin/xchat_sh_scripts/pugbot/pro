if [ "$CHANNEL" = "#siegepug" ]
then echo "!promote"
fi
if [ "$CHANNEL" = "#pug.up" ]
then echo "!promote"
fi

## TODO: Shouldn't we always do this?
if [ "$CHANNEL" = "#crazypug" ]
then pugbot/promote
fi
