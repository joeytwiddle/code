package visualjava.objecteditingcomponents;


public class SimpleFormView extends FormView {

    // Options (object?):
    // formShowChildObjects (otherwise only shows primitives)
    
    public SimpleFormView(Object targetObj) {
        init(targetObj);
    }

    void init(Object targetObject) {
        // @todo
        // for (properties)
        // addThingEditor(property)
        // A thingeditor shows the name, and an edit control for the property.
        // (And a hidden extra-editor O_o ^^ )
    }
    
}
